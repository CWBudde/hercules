# BASE-01 behavior baseline

Captured on 2026-07-25 before remediation work, against
`afdaa2c68bc937b2a11b2e2cc46e3eae3baeb1de`.

## Environment

| Property                     | Value                          |
| ---------------------------- | ------------------------------ |
| Go                           | `go1.26.1`                     |
| OS                           | Ubuntu 24.04.4 LTS             |
| Kernel                       | Linux 6.8.0-136-generic x86_64 |
| Go target                    | `linux/amd64`                  |
| Default `go env CGO_ENABLED` | `1`                            |
| Baseline test cgo setting    | `CGO_ENABLED=0`                |
| golangci-lint                | `2.12.2`, built with Go 1.26.2 |

## Required command results

| Command                                                                                 | Result                | Summary                                                                                                  |
| --------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------- |
| `CGO_ENABLED=0 go test -count=1 ./...`                                                  | Fail                  | `internal/core` failed in the named tests listed below; the remaining packages completed successfully.   |
| `CGO_ENABLED=0 go test -count=20 ./internal/toposort`                                   | Fail                  | `TestToposortBreadthSort` failed on a nondeterministic ordering/level result.                            |
| `CGO_ENABLED=0 go test -race ./internal/core ./internal/plumbing/... ./leaves/...`      | Configuration failure | The Go tool reports `-race requires cgo; enable cgo by setting CGO_ENABLED=1` before compiling packages. |
| `CGO_ENABLED=0 go vet ./...`                                                            | Pass                  | No findings.                                                                                             |
| `CGO_ENABLED=0 golangci-lint run --config ./.golangci.toml --modules-download-mode=mod` | Pass                  | `0 issues.`                                                                                              |

The required race command is retained above as part of the historical baseline. The usable
race-targeted command for this cgo-free renderer repository is:

```bash
CGO_ENABLED=1 go test -tags purego -race \
  ./internal/core ./internal/plumbing/... ./leaves/...
```

`CGO_ENABLED=1` enables Go's race runtime, while `-tags purego` keeps the renderer on its supported
non-cgo implementation. That companion command reached the tests: plumbing and leaves passed, core
failed in the same named baseline tests below, and the race detector reported no data race.

## Named baseline failures

No test in `internal/core` or `internal/toposort` is skipped. The failures are exposed by these
specific test names:

- `TestTracebackMerges/sets_NextMerge_on_commits`
- `TestTracebackMerges/emerge_clears_tracked_merges_for_earlier_commits`
- `TestMergeTrackingNextMerge`
- `TestMergeTrackingNextMergeAfterLastMerge`
- `TestPipelineRun`
- `TestPipelineDryRun`
- `TestPipelineDumpPlanConfigure`
- `TestToposortBreadthSort`

The core failures reproduce with:

```bash
CGO_ENABLED=0 go test -count=1 ./internal/core \
  -run '^(TestTracebackMerges|TestMergeTrackingNextMerge|TestMergeTrackingNextMergeAfterLastMerge|TestPipelineRun|TestPipelineDryRun|TestPipelineDumpPlanConfigure)$'
```

The topological-sort failure reproduces with:

```bash
CGO_ENABLED=0 go test -count=20 ./internal/toposort \
  -run '^TestToposortBreadthSort$'
```

## Package coverage

Coverage was captured with `CGO_ENABLED=0`, `-count=1`, and Go's statement coverage.

| Area           | Package                            | Coverage | Result                                   |
| -------------- | ---------------------------------- | -------: | ---------------------------------------- |
| Core           | `./internal/core`                  |    81.6% | Fail because of the named baseline tests |
| Plumbing       | `./internal/plumbing`              |    86.6% | Pass                                     |
| Plumbing       | `./internal/plumbing/ast`          |    85.8% | Pass                                     |
| Plumbing       | `./internal/plumbing/identity`     |    84.9% | Pass                                     |
| Plumbing       | `./internal/plumbing/imports`      |    82.1% | Pass                                     |
| Plumbing       | `./internal/plumbing/imports/lang` |     0.0% | No test files                            |
| Leaves         | `./leaves`                         |    79.4% | Pass                                     |
| Leaves         | `./leaves/research`                |    78.6% | Pass                                     |
| CLI            | `./cmd/hercules`                   |    22.8% | Pass                                     |
| CLI            | `./cmd/labours`                    |    19.2% | Pass                                     |
| Readers        | `./internal/render/readers`        |    55.8% | Pass                                     |
| Renderer       | `./internal/render`                |    42.1% | Pass                                     |
| Renderer       | `./internal/render/burndown`       |    25.6% | Pass                                     |
| Renderer       | `./internal/render/graphics`       |    29.4% | Pass                                     |
| Renderer modes | `./internal/render/modes`          |    59.7% | Pass                                     |
| Renderer       | `./internal/render/progress`       |     0.0% | No test files                            |

Reproduce the coverage snapshot with:

```bash
CGO_ENABLED=0 go test -count=1 -cover ./internal/core
CGO_ENABLED=0 go test -count=1 -cover ./internal/plumbing/...
CGO_ENABLED=0 go test -count=1 -cover ./leaves/...
CGO_ENABLED=0 go test -count=1 -cover ./cmd/hercules ./cmd/labours
CGO_ENABLED=0 go test -count=1 -cover ./internal/render/readers
CGO_ENABLED=0 go test -count=1 -cover \
  ./internal/render ./internal/render/burndown ./internal/render/graphics \
  ./internal/render/modes ./internal/render/progress
```

## Production-shaped lifecycle fixture

`TestPipelineLifecycleCommitsIntegration` runs a real commit from the checked-in Siva repository
through a deployed `CommitsAnalysis`. Pipeline initialization resolves and executes real upstream
items, covering:

```text
Configure → ConfigureUpstream → Initialize → Consume → Finalize → Serialize
```

The test verifies both YAML and protobuf serialization and is network-free:

```bash
CGO_ENABLED=0 go test -count=1 ./internal \
  -run '^TestPipelineLifecycleCommitsIntegration$'
```
